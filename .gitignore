# Binaries
bin/
/mcpvessel
/cage-init
/directive-sidecar
/findings-sidecar
/payload-proxy
*.cage
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test
*.test
*.out
coverage.txt

# Go
vendor/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.claude
.docs
LAUNCH.md
DISCOVERY.md
SECURITY-SWEEP.md

# OS
.DS_Store
Thumbs.db

# Environment
.env
*.pem
*.key
*.env

# mcpvessel local state (normally under ~/.mcpvessel; guarded here in case a
# run points VESSEL_HOME at the repo, so a signing key, pinned trust, operator
# secrets, or a registry token can never be committed)
signing-key.json
trust.json
secrets.json
mcpregistry-token.json
history.db
replays/

# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
infra/terraform/deploy/secrets.env

# Generated
api/gateway/

# Node.js / TypeScript SDK
node_modules/
package-lock.json
sdk/typescript/dist/

# Python SDK
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
*.pyc

# Build artifacts
dist/

# Local scratch agents for manual routing e2e (not part of the repo)
sample/
*.agent
# Lima release staging (out of dist/, which goreleaser owns)
.lima-release/

# Generated shell completions (release staging)
completions/
tools/docs-mcp/mcpvessel

# Staged bridge binary for the demo server (regenerated by build)
demo/notes-server/mcpvessel
